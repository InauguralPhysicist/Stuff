# Versions used for the published figures in keller_quantization_report.md
# (revision 10; unchanged since revision 9, which produced the figures).
# Newer versions will almost certainly work, but exact pins
# keep the reported numbers bit-reproducible where seeds allow.
sympy==1.14.0
numpy==2.4.4
scipy==1.17.1
